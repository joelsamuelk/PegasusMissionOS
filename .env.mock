# Mock-mode env for the e2e run. The suite is written against the seeded demo
# workspace; with Supabase configured every request demands a real session and
# no dashboard journey can run.
AI_PROVIDER=mock
CONTROL_PLANE_MOCK=true
