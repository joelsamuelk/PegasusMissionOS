import{readFileSync}from"node:fs";import{join}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(join(process.cwd(),"supabase/migrations/0013_control_plane_support_access.sql"),"utf8");describe("CONTROL-8 schema",()=>{it("binds sessions to actor, customer and organisation",()=>{expect(sql).toContain("requester_id uuid not null");expect(sql).toContain("organisation_id uuid not null");expect(sql).toContain("customer_account_id uuid not null");});it("requires bounded expiry and scope",()=>{expect(sql).toContain("check(expires_at>started_at)");expect(sql).toContain("approved_scope");});it("records allowed and denied access",()=>expect(sql).toContain("outcome in('allowed','denied')"));});
