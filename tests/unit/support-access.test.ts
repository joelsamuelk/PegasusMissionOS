import{describe,expect,it}from"vitest";import{evaluateSupportAccess,type SupportGrant}from"@/lib/control-plane/support-access";const now="2026-01-01T10:00:00Z",grant=(o:Partial<SupportGrant>={}):SupportGrant=>({id:"s",requesterId:"actor",organisationId:"org-a",approvedScope:"troubleshooting",status:"active",expiresAt:"2026-01-01T10:30:00Z",...o});describe("support access",()=>{it("allows an exact active scoped session",()=>expect(evaluateSupportAccess({grant:grant(),actorId:"actor",organisationId:"org-a",classification:"customer_content",now}).allowed).toBe(true));it("denies cross-customer access",()=>expect(evaluateSupportAccess({grant:grant(),actorId:"actor",organisationId:"org-b",classification:"customer_content",now}).reasons).toContain("Support session does not match the exact organisation."));it("denies expired and ended sessions",()=>{expect(evaluateSupportAccess({grant:grant({expiresAt:now}),actorId:"actor",organisationId:"org-a",classification:"customer_content",now}).allowed).toBe(false);expect(evaluateSupportAccess({grant:grant({status:"ended"}),actorId:"actor",organisationId:"org-a",classification:"customer_content",now}).allowed).toBe(false);});it("denies insufficient scope",()=>expect(evaluateSupportAccess({grant:grant({approvedScope:"read_only"}),actorId:"actor",organisationId:"org-a",classification:"customer_content",now}).allowed).toBe(false));it("binds a session to its requester",()=>expect(evaluateSupportAccess({grant:grant(),actorId:"other",organisationId:"org-a",classification:"customer_content",now}).allowed).toBe(false));});
