import {readFileSync} from "node:fs";import {join} from "node:path";import {describe,expect,it} from "vitest";
const sql=readFileSync(join(process.cwd(),"supabase/migrations/0009_control_plane_outreach.sql"),"utf8");
describe("CONTROL-4 schema",()=>{it("makes send requests idempotent",()=>expect(sql).toMatch(/idempotency_key text not null unique/));it("requires approval for initial outbound",()=>{expect(sql).toContain("outreach_send_approval_guard");expect(sql).toContain("human approval required");});it("enforces suppression and lawful basis at the database boundary",()=>{expect(sql).toContain("contact compliance blocks outreach");expect(sql).toContain("legitimate-interests reasoning required");});it("keeps contact-source provenance mandatory",()=>{expect(sql).toContain("contact_source_url text not null");expect(sql).toContain("contact_source_retrieved_at timestamptz not null");});});
