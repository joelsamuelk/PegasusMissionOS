import{readFileSync}from"node:fs";import{join}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(join(process.cwd(),"supabase/migrations/0015_control_plane_ai_flags_system.sql"),"utf8");describe("CONTROL-10 schema",()=>{it("stores no prompt or output content columns",()=>{expect(sql).not.toMatch(/prompt text|output text|content text/);});it("requires observations for asserted system state",()=>expect(sql).toContain("state not in('operational','degraded','outage') or observed_at is not null"));it("requires reasons for customer flag targets",()=>expect(sql).toContain("reason text not null check(length(trim(reason))>0)"));});
