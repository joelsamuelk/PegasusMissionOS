import{readFileSync}from"node:fs";import{join}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(join(process.cwd(),"supabase/migrations/0012_control_plane_customer_health.sql"),"utf8");describe("CONTROL-7 schema",()=>{it("restricts signals to operational metadata",()=>expect(sql).toContain("check(classification='operational_metadata')"));it("preserves computed and effective health",()=>{expect(sql).toContain("computed_category");expect(sql).toContain("effective_category");});it("requires reason and actor for changed health",()=>expect(sql).toContain("override_reason is not null and overridden_by is not null"));});
