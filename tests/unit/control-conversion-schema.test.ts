import{readFileSync}from"node:fs";import{join}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(join(process.cwd(),"supabase/migrations/0010_control_plane_conversion.sql"),"utf8");describe("CONTROL-5 schema",()=>{it("is idempotent per prospect and operation",()=>{expect(sql).toMatch(/prospect_organisation_id uuid not null unique/);expect(sql).toMatch(/idempotency_key text not null unique/);});it("provisions in one database function",()=>{expect(sql).toContain("provision_customer_from_prospect");expect(sql).toContain("only a won opportunity can be converted");});it("creates audit in the same transaction",()=>expect(sql).toContain("'customer.provision'"));it("does not grant direct writes",()=>expect(sql).not.toMatch(/grant (insert|update|all)/));});
