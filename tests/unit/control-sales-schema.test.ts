import{readFileSync}from"node:fs";import{join}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(join(process.cwd(),"supabase/migrations/0008_control_plane_qualification_pipeline_tasks.sql"),"utf8");describe("CONTROL-3 schema",()=>{it("requires lost reasons",()=>expect(sql).toMatch(/stage<>'lost' or lost_reason is not null/));it("stores computed and effective qualification separately",()=>{expect(sql).toContain("computed_category");expect(sql).toContain("effective_category");expect(sql).toContain("override_reason");});it("restricts writes by internal role",()=>expect(sql).toMatch(/internal_has_role/));});
