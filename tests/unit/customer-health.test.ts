import{describe,expect,it}from"vitest";import{calculateCustomerHealth,CUSTOMER_HEALTH_VERSION}from"@/lib/control-plane/customer-health";const now="2026-03-10T00:00:00Z",created="2026-01-01T00:00:00Z";describe("customer health",()=>{it("is deterministic and versioned",()=>{const a=calculateCustomerHealth({accountCreatedAt:created,now,onboardingStatus:"completed",setupCompletedAt:"2026-01-02T00:00:00Z",activatedAt:"2026-01-03T00:00:00Z",lastValueAt:"2026-03-01T00:00:00Z"});expect(a).toEqual(calculateCustomerHealth({accountCreatedAt:created,now,onboardingStatus:"completed",setupCompletedAt:"2026-01-02T00:00:00Z",activatedAt:"2026-01-03T00:00:00Z",lastValueAt:"2026-03-01T00:00:00Z"}));expect(a.methodologyVersion).toBe(CUSTOMER_HEALTH_VERSION);expect(a.category).toBe("healthy");});it("explains overdue activation and absent value",()=>{const r=calculateCustomerHealth({accountCreatedAt:created,now,onboardingStatus:"in_progress"});expect(r.score).toBe(35);expect(r.reasons.map(x=>x.signal)).toEqual(["setup_overdue","activation_overdue","no_value_event"]);expect(r.missingInformation).toContain("customer value activity");});it("does not accept tenant content as input",()=>expect(Object.keys(calculateCustomerHealth({accountCreatedAt:created,now}))).not.toContain("content"));});
