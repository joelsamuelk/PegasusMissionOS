import{readFileSync}from"node:fs";import{join}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(join(process.cwd(),"supabase/migrations/0011_control_plane_onboarding_activation.sql"),"utf8");describe("CONTROL-6 schema",()=>{it("separates setup from value",()=>{expect(sql).toContain("onboarding_steps");expect(sql).toContain("customer_value_events");});it("supports configurable criteria",()=>{expect(sql).toContain("activation_criteria");expect(sql).toContain("threshold int");});it("stores time to value",()=>expect(sql).toContain("time_to_value_seconds"));});
