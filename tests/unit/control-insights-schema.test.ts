import{readFileSync}from"node:fs";import{join}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(join(process.cwd(),"supabase/migrations/0014_control_plane_usage_feedback.sql"),"utf8");describe("CONTROL-9 schema",()=>{it("enforces the allow-list in PostgreSQL",()=>{expect(sql).toContain("is_privacy_safe_usage_event");expect(sql).toContain("else false");});it("keeps aggregates customer-scoped",()=>expect(sql).toContain("group by customer_account_id,event_name"));it("bounds feedback summaries",()=>expect(sql).toContain("length(summary) between 1 and 500"));});
