export type SupportScope="read_only"|"troubleshooting"|"elevated";export type ResourceClassification="operational_metadata"|"customer_content"|"customer_sensitive"|"dangerous_administration";const rank:Record<SupportScope,number>={read_only:1,troubleshooting:2,elevated:3};const required:Record<ResourceClassification,SupportScope>={operational_metadata:"read_only",customer_content:"troubleshooting",customer_sensitive:"elevated",dangerous_administration:"elevated"};
export interface SupportGrant{id:string;requesterId:string;organisationId:string;approvedScope:SupportScope;status:"active"|"ended"|"revoked"|"expired";expiresAt:string}
export function evaluateSupportAccess(input:{grant:SupportGrant|null;actorId:string;organisationId:string;classification:ResourceClassification;now:string}){const reasons:string[]=[];const g=input.grant;if(!g)reasons.push("No support session exists.");else{if(g.requesterId!==input.actorId)reasons.push("Support session belongs to another internal user.");if(g.organisationId!==input.organisationId)reasons.push("Support session does not match the exact organisation.");if(g.status!=="active")reasons.push("Support session is not active.");if(new Date(g.expiresAt).getTime()<=new Date(input.now).getTime())reasons.push("Support session has expired.");if(rank[g.approvedScope]<rank[required[input.classification]])reasons.push("Approved support scope is insufficient for this resource.");}return{allowed:reasons.length===0,reasons,requiredScope:required[input.classification]};}
