import type { ContactCompliance,OutreachSendRequest } from "@/server/control-plane/types";
export interface OutreachDecision{allowed:boolean;reasons:string[]}
export function evaluateOutreachCompliance(request:OutreachSendRequest,compliance:ContactCompliance|null):OutreachDecision{const reasons:string[]=[];if(!compliance)reasons.push("Contact provenance and compliance state are missing.");else{if(!compliance.contactSourceUrl||!compliance.contactSourceRetrievedAt)reasons.push("Contact-source provenance is incomplete.");if(compliance.doNotContact)reasons.push("Contact is suppressed.");if(compliance.unsubscribedAt)reasons.push("Contact has unsubscribed.");if(compliance.lawfulBasis==="none_recorded")reasons.push("No lawful basis is recorded.");if(compliance.lawfulBasis==="legitimate_interests"&&!compliance.lawfulBasisNote?.trim())reasons.push("Legitimate-interests reasoning is missing.");}if(request.initialOutbound&&!request.approvedBy)reasons.push("Initial outbound communication requires human approval.");return{allowed:reasons.length===0,reasons};}
