const schemas={signed_in:{method:["password","magic_link","sso"]},funding_assessment_generated:{durationMs:"number",sourceCount:"number"},application_answer_approved:{revisionCount:"number"},report_generated:{sectionCount:"number"},team_member_invited:{role:["owner","administrator","funding_lead","programme_lead","finance_contributor","trustee_reviewer","contributor"]},evidence_added:{evidenceType:["document","metric","financial","policy"]}}as const;export type UsageEventName=keyof typeof schemas;
export function validateUsageEvent(name:string,properties:Record<string,unknown>){if(!(name in schemas))return{valid:false,reason:"Event name is not allow-listed."};const schema=schemas[name as UsageEventName]as Record<string,"number"|readonly string[]>;if(Object.keys(properties).some(k=>!(k in schema))||Object.keys(schema).some(k=>!(k in properties)))return{valid:false,reason:"Event properties do not match the allow-listed schema."};for(const[k,rule]of Object.entries(schema)){const value=properties[k];if(rule==="number"?(typeof value!=="number"||!Number.isFinite(value)):!rule.includes(value as string))return{valid:false,reason:`Invalid property: ${k}.`};}return{valid:true as const};}
