import Link from "next/link";
import { demoProcesses } from "@/lib/process-intelligence";

export default function ProcessesPage() { return <div className="space-y-6"><header><p className="eyebrow">Process Intelligence</p><h1 className="mt-2 text-3xl font-semibold">Captured processes</h1><p className="mt-2 text-sm text-ink-muted">Filter evidence and recommendations without losing sight of the participant’s account.</p></header>
 <form className="surface-card grid gap-3 p-4 sm:grid-cols-3 lg:grid-cols-6">{["Department","System","Score","Annual effort","Review state","Risk"].map(x=><select key={x} className="rounded-lg border border-line bg-white px-3 py-2 text-sm" aria-label={x}><option>{x}: All</option></select>)}</form>
 <section className="surface-card overflow-x-auto"><table className="w-full min-w-[900px] text-left text-sm"><thead className="border-b border-line bg-paper text-xs uppercase tracking-wide text-ink-muted"><tr>{["Process","Department","Frequency","Annual effort","People","Systems","Score","Review"].map(x=><th key={x} className="px-4 py-3">{x}</th>)}</tr></thead><tbody className="divide-y divide-line">{demoProcesses.map(p=><tr key={p.id}><td className="px-4 py-3"><Link className="font-semibold hover:text-blue" href={`/control/processes/${p.id}`}>{p.name}</Link></td><td className="px-4 py-3">{p.department}</td><td className="px-4 py-3 capitalize">{p.frequency.replaceAll("_"," ")}</td><td className="px-4 py-3">≈ {p.annualHours}h</td><td className="px-4 py-3">{p.peopleCount}</td><td className="px-4 py-3 text-xs">{p.systems.join(", ")}</td><td className="px-4 py-3"><span className="rounded-full bg-blue-soft px-2 py-1 font-semibold">{p.score}</span></td><td className="px-4 py-3 capitalize">{p.reviewState.toLowerCase().replaceAll("_"," ")}</td></tr>)}</tbody></table></section></div> }
